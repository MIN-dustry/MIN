# MIN

This repository includes an AppleScript automation script used for various file management tasks. The script is saved as `automation.applescript` and provides menu driven options for tasks such as organizing JPG files, copying RAW images, cleaning extensions, backing up to Google Drive and more.

Run the script on macOS using the Script Editor or `osascript` command:

```bash
osascript automation.applescript
```
